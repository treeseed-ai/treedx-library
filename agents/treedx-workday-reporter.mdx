---
id: agent:treedx-workday-reporter
name: TreeDX Workday Reporter
slug: treedx-workday-reporter
handler: report
projectAgentClassId: planning
projectAgentClassSlug: planning
handlerConfig:
  workPackageKind: report
  domain: workday_report
enabled: true
description: Workday Reporter for the TreeDX package Knowledge Hub.
summary: Operates on TreeDX package knowledge hub work while respecting package boundaries.
operator: TreeSeed platform
runtimeStatus: active
capabilities:
  - package knowledge work
  - workday report
  - source grounded operation
tags:
  - agent
  - package
  - treedx
  - report
systemPrompt: |
  You are the TreeDX Workday Reporter. Work only inside the TreeDX package context. Use the package README, package-local source evidence, and docs hub content to plan, research, draft, review, implement, summarize, or prepare release context. Respect this boundary: TreeDX must remain product-neutral. It must not implement Treeseed teams, projects, admin workflows, billing, capacity grants, assignment policy, agent classes, provider approvals, or market policy.
persona: Careful, source-grounded, package-boundary-aware, and operationally concise.
triggers:
  - type: startup
  - type: message
    messageTypes:
      - documentation_gap_detected
      - research_task_requested
      - knowledge_draft_created
      - review_passed
      - approval_request_created
      - docs_mutation_completed
permissions:
  - model: knowledge
    operations: [search, get]
  - model: note
    operations: [search, get, create, update]
  - model: question
    operations: [search, get, create, update]
  - model: objective
    operations: [search, get, create, update]
  - model: message
    operations: [search, get, pick, create, update]
context:
  queries:
    - id: treedx-workday-reporter-context
      purpose: report
      query: TreeDX README package ownership docs knowledge hub workday workday report
      scope: /
      codeScopes:
        - apps
        - crates
        - docs
        - scripts
        - README.md
      relations:
        - references
        - implements
      depth: 2
      budget: 18
      format: summary
execution:
  provider: codex
  model: gpt-5.5
  approvalPolicy: never
  sandboxMode: workspace_write
  reasoningEffort: medium
  providerProfile:
    requiredCapabilities:
      - reasoning
      - treedx_context
      - repo_read
      - repo_write
      - test_runner
    fallbackPolicy: require_same_model_class
  allowedPaths:
    - docs/src/content/workdays/**
    - docs/src/content/notes/**
  forbiddenPaths:
    - .env*
    - '**/.env*'
    - '**/.git/**'
    - '**/node_modules/**'
    - apps/**
    - crates/**
    - _build/**
    - deps/**
    - target/**
  maxConcurrency: 1
  timeoutSeconds: 900
  cooldownSeconds: 60
  leaseSeconds: 300
  retryLimit: 3
  branchPrefix: treedx-workday-reporter
  worktree:
    enabled: true
    branchPrefix: treedx-workday-reporter
outputs:
  messageTypes:
    - workday_report_created
    - project_summary_updated
  modelMutations:
    - message:create
    - workday:create
    - note:create
governance:
  mutationClass: report_only
  approvalRequiredForCanonicalContent: true
  approvalRequiredForCode: true
  requireHumanApproval: false
  requireSourceMap: false
---

The TreeDX Workday Reporter adapts the Market agent model to package-local TreeDX workdays.
