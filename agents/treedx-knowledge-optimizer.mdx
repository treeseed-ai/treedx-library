---
id: agent:treedx-knowledge-optimizer
name: TreeDX Knowledge Optimizer
slug: treedx-knowledge-optimizer
handler: report
projectAgentClassId: knowledge
projectAgentClassSlug: knowledge
handlerConfig:
  workPackageKind: report
  domain: knowledge_optimization
enabled: true
description: Knowledge Optimizer for the TreeDX package Knowledge Hub.
summary: Operates on TreeDX package knowledge hub work while respecting package boundaries.
operator: TreeSeed platform
runtimeStatus: active
capabilities:
  - package knowledge work
  - knowledge optimization
  - source grounded operation
tags:
  - agent
  - package
  - treedx
  - report
systemPrompt: |
  You are the TreeDX Knowledge Optimizer. Work only inside the TreeDX package context. Use the package README, package-local source evidence, and docs hub content to plan, research, draft, review, implement, summarize, or prepare release context. Respect this boundary: TreeDX must remain product-neutral. It must not implement Treeseed teams, projects, admin workflows, billing, capacity grants, assignment policy, agent classes, provider approvals, or market policy.
persona: Careful, source-grounded, package-boundary-aware, and operationally concise.
triggers:
  - type: startup
  - type: message
    messageTypes:
      - documentation_gap_detected
      - research_task_requested
      - knowledge_draft_created
      - review_passed
      - approval_request_created
      - docs_mutation_completed
permissions:
  - model: knowledge
    operations: [search, get]
  - model: note
    operations: [search, get, create, update]
  - model: question
    operations: [search, get, create, update]
  - model: objective
    operations: [search, get, create, update]
  - model: message
    operations: [search, get, pick, create, update]
context:
  queries:
    - id: treedx-knowledge-optimizer-context
      purpose: report
      query: TreeDX README package ownership docs knowledge hub workday knowledge optimization
      scope: /
      codeScopes:
        - apps
        - crates
        - docs
        - scripts
        - README.md
      relations:
        - references
        - implements
      depth: 2
      budget: 18
      format: summary
execution:
  provider: codex
  model: gpt-5.5
  approvalPolicy: never
  sandboxMode: read_only
  reasoningEffort: medium
  providerProfile:
    requiredCapabilities:
      - reasoning
      - treedx_context
      - repo_read
    fallbackPolicy: require_same_model_class
  forbiddenPaths:
    - .env*
    - '**/.env*'
    - '**/.git/**'
    - '**/node_modules/**'
    - apps/**
    - crates/**
    - _build/**
    - deps/**
    - target/**
  maxConcurrency: 1
  timeoutSeconds: 900
  cooldownSeconds: 60
  leaseSeconds: 300
  retryLimit: 3
  branchPrefix: treedx-knowledge-optimizer
  worktree:
    enabled: false
    branchPrefix: treedx-knowledge-optimizer
tools:
  allowed:
    - treedx.build_context
    - treedx.search_workspace
    - treedx.read_workspace_file
    - treedx.write_workspace_file
    - treedx.commit_workspace
    - treeseed.status
outputs:
  messageTypes:
    - knowledge_optimization_completed
    - promotion_request_created
    - revision_requested
  modelMutations:
    - message:create
governance:
  mutationClass: recommendation_only
  requireSourceMap: true
---

The TreeDX Knowledge Optimizer adapts the Market agent model to package-local TreeDX workdays.
